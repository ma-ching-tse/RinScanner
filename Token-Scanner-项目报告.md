# Token Scanner 项目报告

> 设计规范检查(design-spec-lint)Figma 插件 + 使用统计后台
> 更新于 2026-06-02

---

## 产品概述

Token Scanner 是一款 Figma 设计规范检查插件:一键扫描选中画板,自动揪出未绑定 token 的颜色 / 字体、不规范的布局和默认命名并支持一键修复,让设计稿"干净"到能被 AI 直接转成规范代码。配套的使用统计后台让管理者随时掌握团队采纳情况(谁在用、用了多少、修复率多少),为推广落地提供数据支撑。

---

## 1. 解决什么痛点

**核心问题:Figma 设计稿转代码(AI / MCP codegen)质量差,根因在设计稿本身不规范。** 具体三类:

- **Token 没绑定** — 颜色 / 字体是硬编码,出码全是 `#03b2bd` 而非 `var(--color-bm-*)`,设计 ↔ 代码对不齐、反复返工;
- **布局不规范** — Group、无 auto-layout,出码结构混乱;
- **命名是默认值** — `Frame 427` 之类,出码毫无语义。

过去只能靠人肉 review,不可持续。**Token Scanner 把它工具化**:选区一键扫描 → 三轴(Token / 布局 / 命名)检测 → 一键修复(绑定 Figma 变量、AI 语义命名),让设计稿"干净"到 AI 能直接产出规范代码。

**使用统计**解决的是另一个痛点:**推广时看不见采纳情况** —— 谁在用、用了多少、发现的问题有没有真被修。现在有了**记名 + 修复率**数据支撑推广决策。

---

## 2. 操作指引

### 设计师 — 用插件

1. 选中要检查的画板 / 节点
2. 运行 **Token Scanner** → 点「扫描」
3. 对发现的问题逐个「应用 token」/「改名」
4. (可选)在设置里填入 LLM API Key,启用 AI 语义命名

> 零额外配置,使用数据自动、静默上报。

### 管理者 — 看数据

1. 打开服务器的「远程连接」终端
2. 执行:
   ```bash
   cd ~/token-scanner && node report.js
   ```
   常用参数:
   ```bash
   node report.js --days 7      # 最近 7 天
   node report.js --user 张三   # 只看某人
   ```
3. 查看:使用人数、扫描次数、**修复率**、按人 / 按文件明细、最近事件

### 分发

发布为 **Figma 组织私有插件**(推荐),同事零配置即可用上新版,数据自动汇总到服务器。

---

## 3. 未来进一步计划

- **分发收口** — 发布 org 私有插件,全员统一版本(否则数据为零)
- **A/B demo 收尾** — 由研发在真实 MCP 管线复现 before / after,用客观结果说服团队
- **检测能力扩展** — 更多规则轴、阈值告警、白名单细化
- **数据维度** — 接入团队 / 项目维度与趋势看板;提供匿名模式开关以备合规之需
- **生态整合** — 并入「Rin 生态」品牌(RinScanner),与其它工具打通

---

## 几点想法 💡

1. **修复率是北极星指标。** "发现多少"只说明工具在跑,"修了多少"才说明真正被信任。盯这个数推广,比盯安装量实在。
2. **分发是当前最大瓶颈,不是功能。** 工具再好,同事不升级就一切归零 —— 优先级应高于加新检测。
3. **数据能反哺设计规范。** "问题类型分布"是一份天然的团队培训清单:大家最常犯哪类错,就针对性地讲哪类 —— 工具顺带变成规范教育的抓手。
4. **安全债记得还。** 服务器的"全部 TCP 全开"防火墙规则 + http 明文上报,内部用没问题,但范围扩大前建议收窄端口 + 上 https。
5. **长期愿景:从"lint 工具"走向"设计规范守门人"。** 终极形态是 CI 化 —— 设计稿合并 / 交付前自动扫描,像代码 lint 一样,把规范从"靠自觉"变成"流程保证"。
